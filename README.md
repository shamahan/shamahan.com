# shamahan.com

The personal page at https://shamahan.com/ — a name, a role line,
three profile links and one project. One static HTML file in `site/`,
no build step, no JavaScript, one self-hosted font.

Pushing to `main` publishes `site/` to GitHub Pages through
`.github/workflows/deploy.yml`. Nothing outside `site/` is published.
